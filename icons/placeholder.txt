图标文件占位符

请在此文件夹中添加以下三个PNG图标文件：

1. icon16.png (16x16 像素)
2. icon48.png (48x48 像素)
3. icon128.png (128x128 像素)

建议使用渐变紫色主题以匹配插件界面设计。

在添加图标文件之前，您需要：
1. 创建或下载三个不同尺寸的PNG图标
2. 将它们重命名为上述文件名
3. 放入此文件夹

临时解决方案：
- 可以使用任何PNG图片作为临时图标
- 使用在线工具调整尺寸到所需大小
- 重命名并保存到此文件夹

注意：没有图标文件时，插件仍然可以正常工作，只是不会显示自定义图标。
